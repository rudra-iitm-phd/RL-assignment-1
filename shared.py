import numpy as np

state_space_size = None 
n_actions = None 
bounds = None 

dimension = None
Q = None

# dimension = [i for i in state_space_size] + [n_actions]

# Q = np.zeros([i for i in dimension])